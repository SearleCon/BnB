<h3>Upload photo</h3>
<%= simple_form_for Photo.new, :url => photos_path, :html => { :multipart => true }, :remote => true do |f| %>
    <%= f.error_notification %>
    <%= f.input :description %>
    <%= f.file_field :image %>
<% end %>
