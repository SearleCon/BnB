$('#flash').html('<%= escape_javascript(render :partial => 'shared/flash_messages')%>');
<% if @room.destroyed? %>
$("[rel=tooltip]").tooltip('hide');
$("#<%=@room.id%>").fadeOut(1000, function () {
    $(this).remove();
});
<% end %>
