$('#flash').html('<%= escape_javascript(render :partial => 'shared/flash_messages')%>');
<% if @booking.destroyed? %>
$.lazybox.close();
$('#calendar').fullCalendar('removeEvents', <%= @booking.event.id %>);
<% end %>