$('#flash').html('<%= escape_javascript(render :partial => 'shared/flash_messages')%>');
<% if @line_item.destroyed? %>
$("#<%=@line_item.id%>").fadeOut(1000,function()
{
    $(this).remove();
});
$('#Total_Cost').html('<%= escape_javascript(render :partial => 'bookings/total_cost')%>');
<% end %>
